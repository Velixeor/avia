databaseChangeLog:
  - changeSet:
      id: 1.3
      author: postgres
      changes:
        - sql:
            sql: |
              CREATE TABLE airport.option (
                  id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
                  price DECIMAL(10, 2) NOT NULL,
                  available BOOLEAN DEFAULT true NOT NULL
              );
